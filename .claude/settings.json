{
  "//": "Hooks run stdlib Python via pyrun --isolated (isolated uv/pixi/system; never the project env, so a broken project can't disable the gate/guard). Code/tests use the project env via ## Verify. Both hooks no-op outside an active phase run, except the headless-Claude block which is always on. Windows: replace 'sh .claude/hooks/pyrun.sh' with '.claude\\hooks\\pyrun.cmd'.",
  "hooks": {
    "SubagentStop": [
      { "matcher": "", "hooks": [ { "type": "command", "command": "sh .claude/hooks/pyrun.sh --isolated .claude/hooks/gate.py" } ] }
    ],
    "PreToolUse": [
      { "matcher": "Bash|PowerShell|Edit|Write|NotebookEdit", "hooks": [ { "type": "command", "command": "sh .claude/hooks/pyrun.sh --isolated .claude/hooks/guard.py" } ] }
    ]
  }
}

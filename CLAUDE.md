# Project memory

Phase-orchestration workflow that runs entirely in one interactive Claude Code session
(bills the subscription pool, never headless/API).

On **"start the phase run"**, **"run the next phase"**, or **"orchestrate the plan"**: read
`.claude/workflow.md` and follow it exactly (the full policy). Otherwise, ignore it.

<!-- To auto-load the policy every session, add `@.claude/workflow.md` as the first line (see /memory docs). -->
